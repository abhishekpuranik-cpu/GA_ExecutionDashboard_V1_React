# Render / production: same Atlas cluster URI as Golden Abodes Platform is fine; use a dedicated DB name.
MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=ga_execution_dashboard

# Render sets PORT automatically.
# PORT=10000
